[build]
  command = "yarn build && cd site && yarn build"
  publish = "site/out"

[build.environment]
  NODE_VERSION = "16"
  YARN_VERSION = "1.22.17"
  YARN_FLAGS = "--frozen-lockfile"
  NETLIFY = "true"

[build]
  command = "npm run build && npm run build:site"
  publish = "site/public"

[build.environment]
  NODE_VERSION = "10"
  YARN_VERSION = "1.7.0"
  YARN_FLAGS = "--frozen-lockfile"
  NETLIFY = "true"

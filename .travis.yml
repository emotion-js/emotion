language: node_js

node_js:
  - "7"

cache:
  directories:
    - node_modules

before_install:
  - npm install codecov

install:
  - npm install
  - npm run rollup

after_success:
  - cat ./coverage/lcov.info | ./node_modules/codecov/bin/codecov

cache:
  directories:
    - node_modules

import * as React from 'react'
import styled, { css } from '@emotion/native'

const View = styled.View`
  background: green;
`

const myView = <View />

const Text = styled.Text({ background: 'green' })

const myText = <Text />

const myCss = css`
  background: green;
`

// @flow
import React from 'react'
import Box from '../components/Box'
import Title from '../components/Title'

type Props = {}

const PackagesTemplate = (props: Props) => {
  return <Box>{/* <Title>{props.data.lol}</Title> */}</Box>
}

export default PackagesTemplate

const Image = ({ className, src }) => <img src={src} className={className} />

const Avatar = styled(Image)`
  width: 96px;
  height: 96px;
  border-radius: 50%;
  transition: transform 400ms ease-in-out;

  &:hover {
    transform: scale(1.2);
  }
`

render(<Avatar src={logoUrl} />)
